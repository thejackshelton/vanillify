---
phase: 11-cleanup-and-api-verification
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/pipeline/api-compat.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "convert() without options returns { component, css, themeCss, warnings } with correct types"
    - "convert() with customVariants (CSS string) resolves custom variant classes to correct CSS"
    - "convert() with themeCss (@theme block) resolves theme-defined utilities to correct CSS"
    - "convert() default path produces indexed .nodeN class names and valid CSS"
    - "ConvertOptions accepts customVariants as string or Record<string, string>"
    - "ConvertResult shape includes component, css, themeCss, warnings, optional classMap"
  artifacts:
    - path: "src/pipeline/api-compat.test.ts"
      provides: "API backward compatibility test suite"
      contains: "API-01"
  key_links:
    - from: "src/pipeline/api-compat.test.ts"
      to: "src/index.ts"
      via: "import { convert } from '../index'"
      pattern: "from.*[\"']\\.\\.?/index[\"']"
---

<objective>
Verify public API backward compatibility with dedicated tests for convert() contract.

Purpose: Ensure the v1.x -> v2.0 migration preserved the public API contract. These tests
codify the API shape and behavior guarantees so future changes cannot silently break consumers.

Output: Comprehensive API compatibility test file that passes against the cleaned-up codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@src/types.ts

<interfaces>
<!-- Key types the executor needs from src/types.ts -->

```typescript
export type CustomVariantsOption = string | Record<string, string>;
export type OutputFormat = 'vanilla' | 'css-modules';

export interface ConvertOptions {
  customVariants?: CustomVariantsOption;
  themeCss?: string;
  outputFormat?: OutputFormat;
}

export interface ConvertResult {
  component: string;
  css: string;
  themeCss: string;
  warnings: Warning[];
  classMap?: Record<string, string>;
}

export interface Warning {
  type: "dynamic-class" | "unmatched-class" | "theme-parse-error";
  message: string;
  location: { line: number; column: number };
}
```

From src/index.ts:
```typescript
export async function convert(
  source: string,
  filename: string,
  options?: ConvertOptions,
): Promise<ConvertResult>;

// Public type exports:
export type { ConvertOptions, ConvertResult, OutputFormat, Warning, NodeEntry } from "./types";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API backward compatibility test suite</name>
  <files>src/pipeline/api-compat.test.ts</files>
  <behavior>
    - API-01 (Type shape): convert() returns object with { component: string, css: string, themeCss: string, warnings: Warning[] }. Optional classMap only present when css-modules format.
    - API-04 (Default path): convert(simpleJsx, "test.tsx") without options returns component with .nodeN classes and valid CSS containing those selectors. themeCss is empty string. warnings is empty array.
    - API-02 (customVariants CSS string): convert(jsx, "test.tsx", { customVariants: '@custom-variant ui-checked (&:checked);' }) resolves ui-checked:bg-blue-500 to CSS with :checked selector.
    - API-02 (customVariants Record): convert(jsx, "test.tsx", { customVariants: { 'ui-checked': '&:checked' } }) also resolves correctly (Record form converted internally).
    - API-03 (themeCss @theme block): convert(jsx, "test.tsx", { themeCss: '@theme { --color-brand: #ff0000; }' }) resolves bg-brand to CSS with the brand color.
    - API-03 (themeCss bare declarations): convert(jsx, "test.tsx", { themeCss: '--color-brand: #ff0000;' }) also resolves correctly via the @theme wrapping logic.
    - Unmatched class: convert(jsxWithBogusClass, "test.tsx") produces a warning with type "unmatched-class".
    - Dynamic class: convert(jsxWithTernary, "test.tsx") produces a warning with type "dynamic-class".
  </behavior>
  <action>
    Create `src/pipeline/api-compat.test.ts` with a describe block "API backward compatibility (Phase 11)".

    Test fixtures (inline JSX strings):
    - SIMPLE_JSX: `const Comp = () => <div className="flex items-center p-4">hi</div>;`
    - VARIANT_JSX: `const Comp = () => <div className="ui-checked:bg-blue-500">hi</div>;`
    - THEME_JSX: `const Comp = () => <div className="bg-brand">hi</div>;`
    - UNMATCHED_JSX: `const Comp = () => <div className="not-a-real-class-xyz">hi</div>;`
    - DYNAMIC_JSX: `const Comp = () => <div className={isActive ? "flex" : "hidden"}>hi</div>;`

    Test cases:

    1. "API-01: ConvertResult has correct shape" -- call convert(SIMPLE_JSX, "test.tsx"),
       assert result has keys: component (string), css (string), themeCss (string), warnings (array).
       Assert classMap is NOT present (vanilla format default).

    2. "API-04: default path produces .nodeN classes and valid CSS" -- call convert(SIMPLE_JSX, "test.tsx"),
       assert result.component contains "node0", result.css contains ".node0",
       result.css contains "display" and "flex" and "padding",
       result.themeCss === "", result.warnings.length === 0.

    3. "API-02: customVariants as CSS string resolves custom variants" -- call
       convert(VARIANT_JSX, "test.tsx", { customVariants: '@custom-variant ui-checked (&:checked);' }),
       assert result.css contains ":checked", result.warnings has no unmatched-class for ui-checked:bg-blue-500.

    4. "API-02: customVariants as Record resolves custom variants" -- call
       convert(VARIANT_JSX, "test.tsx", { customVariants: { 'ui-checked': '&:checked' } }),
       assert result.css contains ":checked".

    5. "API-03: themeCss with @theme block resolves theme utilities" -- call
       convert(THEME_JSX, "test.tsx", { themeCss: '@theme { --color-brand: #ff0000; }' }),
       assert result.css contains "background" or "bg", result.warnings has no unmatched-class for bg-brand.

    6. "API-03: themeCss with bare declarations resolves theme utilities" -- call
       convert(THEME_JSX, "test.tsx", { themeCss: '--color-brand: #ff0000;' }),
       assert result.css contains "background" or "bg".
       NOTE: This may fail if tw-generator doesn't auto-wrap bare declarations in @theme {}.
       If it fails, the test documents the limitation and should be marked `.todo()` with
       a comment explaining bare declarations require wrapping. Check current behavior first.

    7. "API-04: unmatched class produces warning" -- call convert(UNMATCHED_JSX, "test.tsx"),
       assert result.warnings.some(w => w.type === "unmatched-class").

    8. "dynamic class produces warning" -- call convert(DYNAMIC_JSX, "test.tsx"),
       assert result.warnings.some(w => w.type === "dynamic-class").

    Import convert from "../index". Use vitest describe/it/expect.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/vanillify && pnpm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 8 API compatibility tests pass. convert() contract verified for:
    default path (API-04), type shape (API-01), customVariants string and Record (API-02),
    themeCss @theme block (API-03), unmatched and dynamic class warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test input -> convert() | Test fixtures are trusted strings -- no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | S (Spoofing) | convert() API | accept | Tests verify API shape but don't test auth -- vanillify is a build-time library with no auth surface |
| T-11-05 | I (Info Disclosure) | warning messages | accept | Warnings contain class names from user's own source code -- not sensitive in build context |
</threat_model>

<verification>
1. `pnpm test` passes with all tests including new api-compat.test.ts
2. Test file contains assertions for API-01, API-02, API-03, API-04
3. No test uses UnoCSS-specific APIs or imports
</verification>

<success_criteria>
- src/pipeline/api-compat.test.ts exists and passes
- Tests cover: default path, type shape, customVariants (string + Record), themeCss, unmatched warnings, dynamic warnings
- All existing tests continue to pass alongside new tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-cleanup-and-api-verification/11-02-SUMMARY.md`
</output>
