---
phase: 11-cleanup-and-api-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/parser.ts
  - src/theme/mapper.ts
  - src/theme/types.ts
  - src/theme/parser.test.ts
  - src/theme/mapper.test.ts
  - src/variants/parser.ts
  - src/variants/resolver.ts
  - src/variants/types.ts
  - src/variants/parser.test.ts
  - src/variants/resolver.test.ts
  - src/pipeline/generator.ts
  - src/pipeline/generator.test.ts
  - src/pipeline/tw-generator.ts
  - src/pipeline/tw-generator.test.ts
  - src/index.ts
  - src/types.ts
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04

must_haves:
  truths:
    - "src/theme/ directory does not exist"
    - "src/variants/ directory does not exist"
    - "src/pipeline/generator.ts is the Tailwind adapter (formerly tw-generator.ts)"
    - "No source file imports from @unocss/core or @unocss/preset-wind4"
    - "Only src/pipeline/generator.ts imports from tailwindcss"
    - "@unocss/core and @unocss/preset-wind4 are not in package.json"
    - "All existing tests pass after cleanup"
  artifacts:
    - path: "src/pipeline/generator.ts"
      provides: "Tailwind compile() adapter (renamed from tw-generator.ts)"
      exports: ["twGenerateCSS", "resetTwGenerator", "_cache"]
    - path: "src/pipeline/generator.test.ts"
      provides: "Tailwind adapter tests (renamed from tw-generator.test.ts)"
      contains: "twGenerateCSS"
  key_links:
    - from: "src/pipeline/rewriter.ts"
      to: "src/pipeline/generator.ts"
      via: "import { twGenerateCSS } from './generator'"
      pattern: "from.*[\"']\\./generator[\"']"
    - from: "src/index.ts"
      to: "src/types.ts"
      via: "type exports"
      pattern: "from.*[\"']\\./types[\"']"
---

<objective>
Delete dead UnoCSS code and dependencies, rename Tailwind adapter to canonical location.

Purpose: Complete the UnoCSS-to-Tailwind migration by removing all UnoCSS code paths and
dependencies. After this plan, the codebase has zero UnoCSS references and a single
Tailwind adapter file at the canonical `pipeline/generator.ts` path.

Output: Clean codebase with only Tailwind engine, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@src/types.ts
@src/pipeline/rewriter.ts
@src/pipeline/tw-generator.ts
@src/pipeline/tw-generator.test.ts
@src/pipeline/generator.ts
@src/pipeline/generator.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code and remove UnoCSS dependencies</name>
  <files>
    src/theme/parser.ts, src/theme/mapper.ts, src/theme/types.ts,
    src/theme/parser.test.ts, src/theme/mapper.test.ts,
    src/variants/parser.ts, src/variants/resolver.ts, src/variants/types.ts,
    src/variants/parser.test.ts, src/variants/resolver.test.ts,
    src/pipeline/generator.ts, src/pipeline/generator.test.ts,
    src/index.ts, src/types.ts, package.json
  </files>
  <action>
    1. Delete entire `src/theme/` directory (CLN-01): `rm -rf src/theme/`
       Contains parser.ts, mapper.ts, types.ts, parser.test.ts, mapper.test.ts.
       Tailwind handles @theme natively via compile() -- these UnoCSS translation layers are dead code.

    2. Delete entire `src/variants/` directory (CLN-02): `rm -rf src/variants/`
       Contains parser.ts, resolver.ts, types.ts, parser.test.ts, resolver.test.ts.
       Tailwind handles @custom-variant natively -- the UnoCSS variant translation layer is dead code.

    3. Delete old UnoCSS generator: `rm src/pipeline/generator.ts src/pipeline/generator.test.ts`
       The old UnoCSS-based generator (createGenerator/preset-wind4) is fully replaced by tw-generator.ts.

    4. Update `src/types.ts` (CLN-02):
       - Remove the import: `import type { CustomVariantsOption } from "./variants/types";`
       - Inline the type definition directly: `export type CustomVariantsOption = string | Record<string, string>;`
       - Also remove the warning types that are UnoCSS-specific and no longer used:
         Keep "dynamic-class", "unmatched-class", "theme-parse-error" in the Warning type union.
         Remove "unknown-theme-namespace" and "unsupported-theme-reset" (these were UnoCSS theme mapper warnings).

    5. Update `src/index.ts`:
       - Remove: `export { parseThemeCss } from "./theme/parser";`
       - Remove: `export { mapToThemeConfig } from "./theme/mapper";`
       These were public exports for UnoCSS theme translation utilities. With native Tailwind,
       users pass raw CSS strings -- no parsing/mapping needed. This is a BREAKING CHANGE
       (v2.0 is a major version, so this is acceptable).

    6. Remove UnoCSS dependencies from package.json (CLN-03):
       - Remove `"@unocss/core": "^66.6.7"` from dependencies
       - Remove `"@unocss/preset-wind4": "^66.6.7"` from dependencies
       - Update `"description"` from "powered by UnoCSS and oxc-parser" to
         "powered by Tailwind CSS and oxc-parser"
       - `tailwindcss: "~4.2.2"` is already present -- verify it remains.

    7. Run `pnpm install` to update the lockfile after dependency removal.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/vanillify && test ! -d src/theme && test ! -d src/variants && test ! -f src/pipeline/generator.test.ts && ! grep -q "@unocss" package.json && echo "PASS: dead code deleted and UnoCSS deps removed"</automated>
  </verify>
  <done>
    src/theme/ and src/variants/ directories deleted. Old UnoCSS generator.ts and generator.test.ts deleted.
    @unocss/core and @unocss/preset-wind4 removed from package.json. types.ts inlines CustomVariantsOption.
    index.ts no longer exports parseThemeCss or mapToThemeConfig.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename Tailwind adapter to canonical generator.ts and fix all imports</name>
  <files>
    src/pipeline/tw-generator.ts, src/pipeline/tw-generator.test.ts,
    src/pipeline/generator.ts, src/pipeline/generator.test.ts,
    src/pipeline/rewriter.ts
  </files>
  <action>
    1. Rename the Tailwind adapter to the canonical path (CLN-04):
       - `mv src/pipeline/tw-generator.ts src/pipeline/generator.ts`
       - `mv src/pipeline/tw-generator.test.ts src/pipeline/generator.test.ts`
       (The old generator.ts was deleted in Task 1, so the path is free.)

    2. Update import in `src/pipeline/rewriter.ts`:
       - Change: `import { twGenerateCSS } from "./tw-generator";`
       - To: `import { twGenerateCSS } from "./generator";`

    3. Update import in `src/pipeline/generator.test.ts` (the renamed file):
       - Change: `import { twGenerateCSS, resetTwGenerator, _cache } from "./tw-generator";`
       - To: `import { twGenerateCSS, resetTwGenerator, _cache } from "./generator";`

    4. Verify isolation (CLN-04): grep the entire src/ directory for `from "tailwindcss"` or
       `from 'tailwindcss'` -- it must appear ONLY in `src/pipeline/generator.ts`.

    5. Run the full test suite: `pnpm test` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/vanillify && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    tw-generator.ts renamed to generator.ts. All imports updated. Only src/pipeline/generator.ts
    imports from tailwindcss. Full test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CSS input -> compile() | themeCss and customVariantsCss strings passed to Tailwind compile() |
| loadStylesheet callback | Virtual filesystem resolver -- only resolves "tailwindcss" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | package.json | mitigate | Verify tailwindcss pinned at ~4.2.2 after removing UnoCSS deps; pnpm-lock.yaml updated |
| T-11-02 | D (DoS) | loadStylesheet | accept | Already bounded in tw-generator.ts (only "tailwindcss" resolves); no change in this plan |
| T-11-03 | I (Info Disclosure) | removed exports | accept | parseThemeCss and mapToThemeConfig removed -- v2.0 major version breaking change is expected |
</threat_model>

<verification>
1. `test ! -d src/theme && test ! -d src/variants` -- dead directories gone
2. `! grep -rq "@unocss" src/` -- no UnoCSS references in source
3. `! grep -q "@unocss" package.json` -- no UnoCSS in deps
4. `grep -c "from.*tailwindcss" src/pipeline/generator.ts` returns 1
5. `grep -rc "from.*tailwindcss" src/ | grep -v ":0$"` shows only generator.ts
6. `pnpm test` passes
</verification>

<success_criteria>
- src/theme/ and src/variants/ directories do not exist
- src/pipeline/generator.ts is the Tailwind adapter (contains compile() import)
- No file in src/ imports from @unocss/core or @unocss/preset-wind4
- Only src/pipeline/generator.ts imports from tailwindcss
- package.json has no @unocss dependencies
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-cleanup-and-api-verification/11-01-SUMMARY.md`
</output>
