---
phase: quick
plan: 260405-c9j
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
must_haves:
  truths:
    - "README displays the vanillify logo centered at the top"
    - "README explains what vanillify is and why it exists"
    - "README shows installation, API usage, and CLI usage"
    - "README includes a before/after Tailwind-to-vanilla-CSS example"
    - "README uses sparse emojis (tasteful, not every heading)"
  artifacts:
    - path: "README.md"
      provides: "Project README with logo, usage docs, and examples"
      min_lines: 100
---

<objective>
Create a polished README.md for the vanillify project.

Purpose: Give users and potential contributors a clear understanding of what vanillify does, why it exists, and how to use it — with the vanillify logo front and center.
Output: README.md at project root
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@package.json
@src/index.ts
@src/types.ts
@src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with full project documentation</name>
  <files>README.md</files>
  <action>
Create README.md at the project root with the following structure and content:

**Logo and title:**
- Center the logo using HTML: `<p align="center"><img src="vanillify-logo.webp" alt="Vanillify" width="200" /></p>`
- Centered title: `<h1 align="center">Vanillify</h1>`
- Centered tagline: `<p align="center">Tailwind CSS to vanilla CSS — powered by UnoCSS's actual CSS generation engine</p>`

**Sparse emoji usage:** Use emojis only on 2-3 section headings max (e.g., the "Why" section, "How it works"). Do NOT put emojis on every heading.

**Sections in order:**

1. **What is Vanillify?** — Brief paragraph: converts Tailwind utility classes in JSX/TSX to vanilla CSS files. Uses UnoCSS's `createGenerator` for real CSS output and `oxc-parser` for proper AST-based extraction. Not regex, not lookup tables.

2. **Before / After example** — Show a simple JSX component with Tailwind classes, then the two output files (.vanilla.tsx with .node0/.node1 classes, .vanilla.css with the actual CSS). Keep the example small (a card component with 3-4 elements using flex, padding, text, bg colors).

3. **Installation**
   ```bash
   npm install vanillify
   ```

4. **Usage — Programmatic API**
   ```ts
   import { convert } from 'vanillify'

   const { component, css, warnings } = await convert(source, 'Card.tsx')
   ```
   Briefly explain: `component` is the rewritten source, `css` is the generated vanilla CSS, `warnings` contains dynamic/unmatched class info.

5. **Usage — CLI**
   ```bash
   npx vanillify "src/components/*.tsx"
   npx vanillify "src/Button.tsx" -o dist
   npx vanillify "src/**/*.tsx" -c custom-variants.css
   ```
   Document the flags: `-o` / `--outDir` for output directory, `-c` / `--customVariants` for custom variant CSS file. Outputs `.vanilla.css` + `.vanilla.tsx` files.

6. **Custom Variants** — Explain that Tailwind v4's `@custom-variant` is supported. Show a small example CSS file with `@custom-variant` definitions and the `-c` flag usage. Also show the programmatic API option form.

7. **How It Works** (can use a small emoji here) — Brief 4-step pipeline:
   1. Parse JSX/TSX with oxc-parser (AST, not regex)
   2. Extract class names from className/class attributes
   3. Generate real CSS via UnoCSS's createGenerator with preset-wind4
   4. Rewrite source with indexed class names (.node0, .node1, ...)

8. **Why not regex?** (can use an emoji) — 2-3 sentences on why AST parsing + real CSS generation matters vs lookup tables/regex. Mention the key differentiator.

9. **Requirements** — Node.js >= 20

10. **License** — MIT

Keep the tone direct and technical. No marketing fluff. The README should feel like it was written by a developer for developers.
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '{if ($1 >= 100) print "PASS"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>README.md exists at project root with logo, all sections, before/after example, API + CLI usage, and sparse emojis. At least 100 lines.</done>
</task>

</tasks>

<verification>
- README.md exists and contains the vanillify logo reference
- All major sections present: what, why, install, API, CLI, custom variants, how it works, license
- Before/after example is realistic and shows .node0/.node1 pattern
- Emojis are sparse (2-3 max, not on every heading)
- No broken markdown syntax
</verification>

<success_criteria>
README.md is complete, well-structured, and accurately documents vanillify's API, CLI, and value proposition with the logo displayed prominently at the top.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-c9j-create-a-readme-for-the-vanillify-projec/260405-c9j-SUMMARY.md`
</output>
