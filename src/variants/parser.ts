import type { ParsedVariant } from './types'

/**
 * Parse @custom-variant shorthand directives from a CSS string.
 * Stub -- implementation pending (TDD RED phase).
 */
export function parseCustomVariantCSS(_css: string): ParsedVariant[] {
  return []
}
